## SwarmZ v0.2.5 — small fix 🩹

### Fixed

- The model badge in pane headers ("Fable 5", "Opus 4.8", …) no longer
  wraps onto two lines when a pane gets narrow — badges stay one line
  everywhere, the pane title truncates instead.
