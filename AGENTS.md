# AGENTS.md

SwarmZ — run and monitor a swarm of native Codex agents (`codex app-server` sessions), organized in project tabs, with an AI Conductor on top. React frontend + native macOS app (Tauri 2 / Rust). Deep per-subsystem details live in `docs/ARCHITECTURE.md` — read the relevant section there (or the code) before changing a subsystem, and keep it in sync.

**Codex-only rebuild (branch `rebuild/codex-only-v1`, Phase 2 done).** The old terminal/PTY/grid subsystem, the Claude runtime, the OpenRouter brain and voice dictation are REMOVED (Phase 1 of `docs/plans/rebuild-codex-only-v1.md`; git history and `main` preserve everything); the projects & swarm data model is in (Phase 2). New features are native-only Codex; do not reintroduce terminal/PTY/web-mode paths.

## Stack & commands

- React 19 + TypeScript + Tailwind v4 (dark only — follow `DESIGN.md`), Zustand, pnpm
- Type-check: `./node_modules/.bin/tsc --noEmit` · Build: `pnpm build` · Dev: `pnpm tauri dev` · Frontend unit tests: `pnpm test` (vitest, pure functions — `src/**/*.test.ts`, no DOM) · Rust tests: `cargo test` in `src-tauri/`

## Map

Frontend (`src/`):

- `store.ts` — the slim shared store: settings (always via `updateSettings`), all-time usage history, quick notes, the worktree panel state, quit-confirm + a few UI flags, and `hydrate()` (schema migration first, then projects BEFORE vibe sessions — the session migration assigns into the project store) / `flushAllPersists()` (which also flushes the vibe + chat + project stores). Sessions, chats and projects live in their own stores.
- `lib/projects/` — the project tabs: `core.ts` (pure: canonical-dir dedupe key, collision-suffixed naming, tab reorder patches, `assignSessionsToProjects` — the schema-v2 session→project migration) + `store.ts` (standalone zustand store, persisted key `projects`; `openProject` dedupes by canonical path via the Rust `canonicalize_path`; closing a tab only stamps `closedAt` — sessions stay, reopening the folder brings them back)
- `lib/transport.ts` → `backend-types.ts` / `backend-tauri.ts` — backend interface (persistence incl. `deleteStoreKeys`, git info, notifications, codex account limits); native-only features may skip it and `invoke` directly (`lib/worktree.ts`, `lib/orchestrator/native.ts`, `lib/vibe/controller.ts`, `lib/projects/store.ts`)
- `lib/quit.ts` — quit guard (`vibeBusyIds` + `flushAllPersists`); `lib/limits.ts` — the Codex account-level subscription meters; `lib/events.ts` — in-memory fleet event feed (Deck ticker); `lib/updates.ts` — auto-updater; `lib/paths.ts` — pure filesystem-path detection for the chat path pills; `lib/schema-version.ts` — the store migration anchor (`CURRENT_SCHEMA_VERSION = 2`, `planSchemaMigration` + the one-time `DEAD_STORE_KEYS` cleanup)
- `lib/orchestrator/` — orchestrator sensing + tool bus + chat plumbing: `snapshot.ts` (pure session snapshot + summary line), `native.ts` (transcript/docs/discovery wrappers; `collectKnownFolders` from project tabs/live sessions/notes/worktree repos), `bus.ts`/`executors.ts` (webview half of the tool bus — session-only executors: `fleet_snapshot`, `read_transcript`, `read_project_docs`, `read_notes`, `git_status` (live `git_info` on a session cwd or path), `list_projects`, `prompt_pane` (busy sessions refuse), `create_panes` (1–8 native sessions, conductor-spawned: project from the spec `cwd`, pool agent names, never steals the active tab), `remember`), `chat.ts` (typed `orchestrator_chat_*` wrappers + event listener), `chat-store.ts` (chat state, standalone zustand store, persisted key `orchestratorChats`), `controller.ts` (chat events → store bridge with delta batching + the session activity-ping watcher, **outside React**), `persona.ts` (Maestro/Hive/Orchestrator presets + `effectivePersona`/`personaWire`), `memory.ts` (typed `orchestrator_memory_*` wrappers), `tool-labels.ts` (pure human activity labels + grouping), `models.ts` (`recentCodexModels` from the usage history + `CODEX_EFFORTS` + the `codex_list_models` catalog store) + dev hook `window.__orch`
- `lib/vibe/` — the native Codex sessions: `session-store.ts` (standalone zustand store, transcript normalized by item id, persisted key `vibeSessions` v2 — sessions carry `projectId`/`agentName`/`spawnedBy`/`worktree`, cap 30 PER PROJECT, hydrate runs the project migration), `controller.ts` (typed `vibe_session_*` wrappers + `vibe://session-event` bridge with delta batching, lazy resume, `vibeBusyIds`, project-aware `focusSession` + `activateProject`(ByIndex), the `token_usage`→usage-history accounting mirror, Deck-event mirroring, **outside React**), `names.ts` (pure agent-name pool: `pickAgentName`, `branchSlugForAgent`), `ui.ts` (pure signal/diff/token helpers), `transcript.ts` (pure `renderSessionTranscript` for the orchestrator), `mention.ts` (pure `@session` parser), `diff.ts` + `diff-worker.ts`/`diff-highlight.ts` (diff parsing/counting + off-thread highlight), `triage.ts` (needs-you ordering → Deck queue, global across projects), `ui-store.ts` (transient dialog + stage-mode state) + dev hook `window.__vibe` (incl. `seed.*`)
- `components/` — `App.tsx` (global shortcuts incl. ⌘1–⌘9 project switch, watchers, dialog mounts; the VibeLayer is the only view), `TitleBar` (project tabs — rename/close/reorder/⚑-badges + the "+" folder-picker/recents dropdown — plus usage/notes/conductor/worktrees/settings buttons + New Session; `data-tauri-drag-region="deep"`), `Deck` (bottom status bar: GLOBAL session triage queue, event ticker, Codex meters, orch dot), `components/vibe/` (`VibeLayer`/`SessionRail` (pinned Conductor card + the ACTIVE project's session cards)/`FocusStage` (Orchestrator-first: `ConductorStage` — the orchestrator chat with an `@session` routing composer — or the selected session)/`ItemFeed` (virtua `VList`)/`Composer` (with the pending-approval takeover)/`DiffCard`/`NewVibeSessionDialog` (active-project default, generated agent name + 🎲 reroll)), `components/orchestrator/ChatView.tsx` (the shared chat message list: reading-width column, grouped activity blocks, path pills, switcher, session jump chips, `ChatMeta` model/effort chip + context gauge) + `ModelEffortPicker.tsx`, `CommandPalette` (⌘K: projects + sessions + actions), `SettingsDialog` (Orchestrator/Updates/Paths/About), `UsageDashboard` (all-time Codex history, fed live by the controller's accounting mirror), `QuickNotesPanel` (⌘N), `WorktreePanel` + `CloseWorktreeDialog` (dormant until Phase 4 wires session-worktree close), `QuitConfirmDialog`

Backend (`src-tauri/src/`):

- `lib.rs` — commands (incl. `canonicalize_path`, the project dedupe resolver), quit/exit handling, store rescue at setup
- `codex_usage.rs` — incremental codex jsonl usage parsing + the shared usage types (merged from the removed `usage.rs`); also serves account-level Codex rate limits (`codex_account_limits`, bounded newest-first tail scan). The `usage_for_*` commands currently have no frontend caller (session accounting mirrors `token_usage` events frontend-side).
- `git.rs` — read-only git status (with subprocess timeouts) · `worktree.rs` — worktree create/status/remove/list (the only git-writing module)
- `storefile.rs` — swarmz.json backup/rescue · `transcript.rs` — codex rollout transcript tails + project docs · `projects.rs` — project discovery, codex-only (rollout `session_meta` cwds, known folders, shallow git scan; feeds the "open project" recents + `list_projects`)
- `codex/` — generic `codex app-server` host, shared by the orchestrator and the sessions: `protocol.rs` (pure wire framing with fixture-frozen 0.144.1 shapes) + `host.rs` (`Client` spawn/stdio/pending-rpc incl. the GUI-PATH fix, binary-resolve cache, per-process thread registry routing server events per `threadId`, lazy-respawn `ProcessHost` slots — orchestrator = ONE shared slot, session = a private slot per thread — and typed `thread/resume` where `ResumeError::ThreadNotFound` = fall back to `thread/start`) + `sessions.rs` (the native sessions: STANDARD codex harness — no dynamicTools/developerInstructions — access→sandbox mapping (workspace/full), the eight `vibe_session_*` commands, event mapping → `vibe://session-event`, remembered approval Responders)
- `orchestrator/` — tool bus + brain: `registry.rs` (the single-source tool catalog, 9 session-only tools) + `persona.rs` (the single-source `build_instructions(persona, memory)`: persona header + the hard-wired operative core + memory snapshot; `PersonaSpec` + the Maestro seed) + `memory.rs` (`orchestrator-memory.md` in the app data dir, FIFO caps 20 entries / 2000 chars) + `bus.rs` (Rust↔webview roundtrip with pending map + per-tool timeouts) + `appserver.rs`/`adapter.rs` (the Codex brain: chats ↔ threads over the shared ProcessHost, registry as experimental dynamicTools, `item/tool/call` answered via the bus, streaming `orchestrator://chat-event`s, auto-declined approvals, transparent thread/resume after respawns; instructions compiled per session via `build_instructions`)

## Load-bearing invariants (don't break these)

- A `useSyncExternalStore`/zustand selector must never return a freshly-built array of fresh objects (even under `useShallow`) — it never stabilizes across `getSnapshot` calls and infinite-loops. Select a **primitive signature** and rebuild the objects in `useMemo` (see the Deck's `useTriageRows`). The rail cards, feed rows, approval takeover and cross-session banner all obey this: each selects only its own item/session by id (or a first-pending-id/count signature), so a streaming delta on one session/item re-renders exactly that row — never the rail or a sibling.
- Session transcript items are **normalized by id** (`items: Record<id, VibeItem>` + `order`); a streaming delta must replace ONLY its own item object so every other item keeps its reference (the identity-preservation lesson — one row re-renders per delta batch). Don't rebuild the whole item array on a patch.
- A native session runs **one turn at a time over one private `codex app-server` process** (`busy` claimed synchronously in `send`); a busy session refuses a new turn — both the controller and the orchestrator's `prompt_pane` enforce this. On close, **every remembered approval `Responder` must be answered** (with `cancel`) before the entry is dropped, or the child process hangs waiting.
- Persistence distinctions: `schemaVersion` is `2` — stores below it get the one-time dead-key cleanup + stamp (`planSchemaMigration`; a NEWER stored version is never clobbered) · closing a project tab only stamps `closedAt` — NEVER delete its sessions; reopening the same folder must resurface them under the SAME project id (dedupe by canonical dir) · pre-v2 sessions hydrate through `adoptSessions` (projects derived from `projectDir`s) — keep that path tolerant · `settings.orchestratorPersona` unset = the Maestro seed (never write the seed out; "Reset" clears back to `undefined`) · orchestrator chats hydrate tolerantly (pre-rebuild `provider`/`wire` fields are dropped; an OpenRouter-era model stamp is cleared).
- Orchestrator instructions come from ONE function, `orchestrator/persona.rs` `build_instructions(persona, memory)`. Its `OPERATIVE_CORE` block is **not** persona-overridable — persona only prepends voice/self-image, it must never be able to weaken a guardrail (never edit files, never push unprompted, the human owns approvals). The Phase-1 core is the codex-only interim edit of the former word-for-word text (pane/layout sentences removed; content tests frozen on the new text); extend it, don't reword — the full v2 core lands in Phase 3. Memory (`orchestrator-memory.md`, FIFO-capped 20/2000) is written only via the explicit `remember` tool — no silent auto-write; the model proposes uncertain facts and stores after confirmation.
- Worktree deletion is gated: silent removal only after a re-check at execution time; "Clean up safe" re-checks each worktree and never touches paths a live session works in; risky rows need a second armed click.
- Global shortcuts early-return while a dialog is open (⌘K may close its palette, ⌘N its drawer); ⌘W is always `preventDefault`ed.
- Sync Tauri commands run on the main thread — anything blocking (subprocesses, file walks) must be async + `spawn_blocking`.
- Quit flushes **all** debounced persists (`flushAllPersists`); the quit/close choreography in `lib/quit.ts` + `lib.rs` looks redundant but every leg is load-bearing (window-state save, updater restart code — the `ExitRequested` handler in `lib.rs` must stay as is).
- Title-bar dragging relies **only** on `data-tauri-drag-region="deep"` on the `<header>` in `TitleBar.tsx` — WKWebView ignores `-webkit-app-region`, so the `drag-region`/`no-drag` CSS classes are no-ops on macOS. `"deep"` makes the whole subtree draggable; Tauri's `drag.js` auto-excludes real `<button>`/`<input>`/`role` elements. The project tabs are plain `<div>`s (activate + close buttons as siblings inside), so they opt out explicitly via `data-tauri-drag-region="false"` — keep that attribute when touching the tab markup. Don't switch back to bare `data-tauri-drag-region` (drags only direct hits on the header itself = just the flex gaps).

## Releases & updates

- Auto-updater polls `latest.json` on GitHub Releases (`lib/updates.ts`; pill in `TitleBar.tsx`, toggle in Settings). About shows `__APP_VERSION__` (Vite define). Builds are ad-hoc signed only.
- Release checklist: bump version in `package.json`, `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml` (keep in sync) → create `docs/release-notes/v<X.Y.Z>.md` (CI fails without it) → tag `v<X.Y.Z>` + push → `.github/workflows/release.yml` publishes incl. updater manifest.
- Updater signing key: `~/.tauri/swarmz_updater.key` (no password), repo secret `TAURI_SIGNING_PRIVATE_KEY`. Losing it breaks updates for existing installs.

## Ground rules

1. **Never start the dev server** or any long-running app process (`pnpm tauri dev`, `pnpm dev`, …) without explicit approval first. Type-checks, builds and `cargo test` are fine.
2. **Never commit or push on your own.** Only when explicitly asked or approved.
3. **Keep docs in sync.** If a change affects anything stated in `AGENTS.md`, `docs/ARCHITECTURE.md` or `README.md`, update them in the same change — proactively, without being asked.
